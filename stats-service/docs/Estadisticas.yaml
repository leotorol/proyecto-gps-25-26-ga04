openapi: 3.0.0
info:
  title: UnderSounds — Estadísticas y Recomendaciones
  version: "1.0.0"
  description: "Endpoints reales expuestos por `stats-service`."
servers:
  - url: http://localhost:5002/api

paths:
  /stats/events:
    post:
      summary: Enviar evento de usuario (ingesta)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Event'
      responses:
        "202":
          description: Evento aceptado
          content:
            application/json:
              schema:
                type: object
                properties:
                  accepted:
                    type: boolean
                    example: true
                  id:
                    type: string
        "400":
          description: Petición inválida
          content:
            application/json:
              schema:
                type: object
                properties:
                  detail:
                    type: string
        "500":
          description: Error interno

  /stats/artist/{artist_id}/kpis:
    get:
      summary: KPIs resumidos para un artista
      parameters:
        - in: path
          name: artist_id
          required: true
          schema:
            type: string
        - in: query
          name: startDate
          schema:
            type: string
            format: date
        - in: query
          name: endDate
          schema:
            type: string
            format: date
      responses:
        "200":
          description: KPIs agregados (plays, likes, follows, purchases, revenue)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArtistKPI'
        "400":
          description: Formato de fecha inválido
        "404":
          description: Artista no encontrado

  /stats/top:
    get:
      summary: Rankings simples por tipo
      parameters:
        - in: query
          name: type
          required: true
          schema:
            type: string
            enum: [track, album, artist]
        - in: query
          name: period
          schema:
            type: string
            enum: [day, week, month, year]
            default: week
        - in: query
          name: limit
          schema:
            type: integer
            default: 10
      responses:
        "200":
          description: Lista ordenada por métricas agregadas
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                    type:
                      type: string
                      enum: [track, album, artist]
                    title:
                      type: string
                      nullable: true
                    metricValue:
                      type: number

  /stats/trending:
    get:
      summary: Tendencias por género o global (heurística)
      parameters:
        - in: query
          name: genre
          schema:
            type: string
            description: "Si 'tracks' o 'artists' devuelve listas específicas"
        - in: query
          name: period
          schema:
            type: string
            enum: [day, week, month]
            default: week
        - in: query
          name: limit
          schema:
            type: integer
            default: 10
      responses:
        "200":
          description: Lista de tendencias (tracks o artists según `genre`)
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object

  /stats/export:
    get:
      summary: Exportar métricas (CSV o JSON)
      parameters:
        - in: query
          name: type
          schema:
            type: string
            enum: [plays, likes, follows, purchases]
            default: plays
        - in: query
          name: startDate
          schema:
            type: string
            format: date
        - in: query
          name: endDate
          schema:
            type: string
            format: date
        - in: query
          name: format
          schema:
            type: string
            enum: [csv, json]
            default: csv
      responses:
        "200":
          description: Archivo con métricas (CSV o JSON)
          content:
            text/csv:
              schema:
                type: string
                format: binary
            application/json:
              schema:
                type: array
                items:
                  type: object

  /stats/alerts:
    post:
      summary: Crear alerta/trigger manual para un artista
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                artistId:
                  type: string
                windowMinutes:
                  type: integer
                thresholds:
                  type: object
                notifyEmail:
                  type: string
      responses:
        "200":
          description: Resultado de evaluación de umbrales / envío de email
          content:
            application/json:
              schema:
                type: object

  /stats/cache/clear:
    post:
      summary: Limpiar caché (todo o clave específica)
      parameters:
        - in: query
          name: key
          schema:
            type: string
          description: "Clave de caché a limpiar (opcional)"
      responses:
        "200":
          description: Resultado de limpieza
          content:
            application/json:
              schema:
                type: object
                properties:
                  cleared:
                    type: string

  /stats/cache/info:
    get:
      summary: Información del caché interno
      responses:
        "200":
          description: Estadísticas del caché
          content:
            application/json:
              schema:
                type: object
                properties:
                  current_size:
                    type: integer
                  max_size:
                    type: integer
                  ttl_seconds:
                    type: integer
                  keys:
                    type: array
                    items:
                      type: string

  /stats/cb/status:
    get:
      summary: Estado del circuit breaker usado para llamadas al content service
      responses:
        "200":
          description: Estado y contadores del circuit breaker
          content:
            application/json:
              schema:
                type: object

  /recommendations/user/{user_id}:
    get:
      summary: Recomendaciones heurísticas para un usuario
      parameters:
        - in: path
          name: user_id
          required: true
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Recomendaciones (lista)
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object

  /recommendations/similar:
    get:
      summary: Recomendaciones similares por género
      parameters:
        - in: query
          name: genre
          required: true
          schema:
            type: string
        - in: query
          name: excludeId
          schema:
            type: string
        - in: query
          name: limit
          schema:
            type: integer
            default: 10
      responses:
        "200":
          description: Lista de ítems similares
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object

components:
  schemas:
    Event:
      type: object
      properties:
        eventType:
          type: string
          example: "track.played"
        timestamp:
          type: string
          format: date-time
        userId:
          type: string
          nullable: true
        anonymous:
          type: boolean
          example: false
        entityType:
          type: string
          nullable: true
        entityId:
          type: string
          nullable: true
        metadata:
          type: object
          additionalProperties: true

    ArtistKPI:
      type: object
      properties:
        artistId:
          type: string
        period:
          type: string
          nullable: true
        plays:
          type: integer
        uniqueListeners:
          type: integer
        likes:
          type: integer
        follows:
          type: integer
        purchases:
          type: integer
        revenue:
          type: number
          format: float